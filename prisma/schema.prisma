generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Test {
  testId Int    @id @default(autoincrement()) @map("test_id")
  test   String

  @@map("test")
}
